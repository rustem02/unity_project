using UnityEngine;

namespace VRTraining.VR
{
    /// <summary>
    /// Marks the player root so zones can detect both XR and desktop avatars.
    /// </summary>
    public class PlayerMarker : MonoBehaviour
    {
    }
}
